user  nginx;

events {
    worker_connections   1000;
}

http {
    upstream neural_network {
        server neural_network:8000;
    }
    server {
        listen 5001;

        location / {
            proxy_set_header Host $host;
            proxy_pass http://neural_network;
        }
    }

}
