const artefacts = require('./artefacts');

const initServices = (config) => {
    // const artefactsService = artefacts(config);

    return {
        artefactsService: {},
    }
}

module.exports = initServices;
